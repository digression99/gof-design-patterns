// import { PersonBuilder } from './person';
import { Client as ModalClient } from './builder-example-1';
// import { Client, Director } from './builder-example-2';

document.addEventListener('DOMContentLoaded', () => {
  // const director = new Director()
  // Client(director);

  ModalClient();
});
